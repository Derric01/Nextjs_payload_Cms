// This file is intentionally exporting the config from the root
// to ensure both config files stay in sync.
// The actual configuration is now in the root payload.config.js file

import config from '../payload.config.js';

export default config;
